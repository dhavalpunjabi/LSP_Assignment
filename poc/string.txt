create table if not exists company (EMP_ID INT NOT NULL PRIMARY KEY, NAME TEXT NOT NULL, AGE INT NOT NULL, CONTACT TEXT NOT NULL, RECORD DATETIME NOT NULL);create table if not exists project (EMP_ID INT NOT NULL, PROJECT_ID INT NOT NULL, PROJECT_NAME TEXT NOT NULL, PROJECT_END_DATE DATE NOT NULL, FOREIGN KEY(EMP_ID) REFERENCES company(EMP_ID));
